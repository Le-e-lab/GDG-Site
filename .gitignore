node_modules/
.DS_Store
.gemini/
*.log

# Local secrets (Supabase access token etc.) — never commit
.env
.env.local

# Raw photo originals (compressed versions are used on the site)
images/*.JPG

# Supabase CLI local state
supabase/.temp/
supabase/.branches/
supabase/.env
